#include "tuple_graph_utils.h"


namespace dlplan::novelty {

TupleGraph canonize(const TupleGraph& tuple_graph) {
    // Step 1: Sort nodes, compute index remapping
    // Step 2: Recreate attributes with new index mapping
}

}